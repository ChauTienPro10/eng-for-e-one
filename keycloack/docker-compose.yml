services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.2
    container_name: keycloak
    # restart: always
    command: start-dev
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://192.168.134.219:5432/keycloak
      KC_DB_USERNAME: admin
      KC_DB_PASSWORD: tien22012003

      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: tien22012003

    ports:
      - "8080:8080"